/**
 * Infrastructure layer: contains implementations of domain port interfaces.
 * Includes JPA repository implementations, external service adapters (LLM, YouTube, Keycloak),
 * and framework-specific configuration.
 */
package com.dopashift.infrastructure
