/**
 * Application layer: contains use cases (command/query handlers) and DTO mappers.
 * Depends on the domain layer. Orchestrates domain logic without framework coupling.
 */
package com.dopashift.application
