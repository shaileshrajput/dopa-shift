package com.dopashift.domain.model

/**
 * Supported user locales for content recommendations and UI localization.
 */
enum class UserLocale(val code: String) {
    EN("en"),
    HI("hi"),
    MR("mr")
}
